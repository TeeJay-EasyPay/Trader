# AT-ED-010 Status

**Status: Implemented, deployed, and verified in production. Requirements 1-5 complete.
`/brokers`/`/status` performance is a documented, honest partial-completion item, not a silent
gap.**

| Requirement | Status |
|---|---|
| 1. Visible staleness/cache indicator | Done — six distinct UI states, wired into every screen's header |
| 2. Timeout/retry reconsideration | Done — bounded single retry implemented; the exact `PRIMARY_REFRESH_TIMEOUT_MS` value itself was left at its existing setting (not retuned), since production measurement showed `/founder-evidence` consistently well under the current timeout — no evidence justified changing the number |
| 3. Surface backend snapshot metadata | Done — evidence timestamp, age, and stale flag shown distinct from mobile cache age |
| 4. Fix `SCHEDULED_JOB_RUNS` query performance | Done, index added and verified — but proven insufficient alone; the dominant cost was a separate bug (see below) |
| 5. Fix `broker_panels()` latency | Partially done — concurrent fetch and batching implemented and verified locally; production still shows `/brokers` at ~59.7s, improved from an unbounded ~60s+ hang but not resolved to a healthy latency |

**Two bugs outside the original 5 requirements were found and fixed because they were blocking
requirement 4 from actually working in production**: a semicolon-in-SQL-comment bug that broke
Postgres schema initialization (self-inflicted, same-day fix), and an unbounded incident-write
loop from historical worker rows in `supervise_workers` (the real cause of the `/status`/
`/phase5-status` hang, now fixed and verified: ~12s, down from an unbounded hang).

## Completion criteria, checked against the directive

- ✅ Founder can distinguish live from cached data in the app itself — implemented, unit-tested.
- ⚠️ `/status`/`/phase5-status`/`/brokers` respond in a bounded, reasonable time — **two of three
  achieved** (`/phase5-status` and, indirectly, `/operations-health`/`/sprint6-status`); `/status`
  and `/brokers` remain slow, root cause only partially identified.
- ✅ No trading, risk, or governance behaviour changed — confirmed via diff review (no
  authoritative domain module touched) and direct production check (Kraken hold/capital ledger
  unchanged throughout).
- ✅ All safety and characterization tests pass — 310/310, run clean multiple times.

## Recommended next step

A dedicated, separate follow-up specifically for `/brokers`/`/status`'s remaining slowness -
this needs its own focused investigation (likely profiling the concurrent-fetch path's actual
production timing per-broker, which requires either Render log access or another round of
temporary instrumentation) rather than being folded into further AT-ED-010 work, since the
Founder-facing mobile app itself does not depend on either endpoint and is already fixed.
