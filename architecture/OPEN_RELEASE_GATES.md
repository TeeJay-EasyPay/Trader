# Open Release Gates

Date: 2026-07-18

## Gates Still Open

1. Supabase/Postgres production connection string is configured in Render.
2. `/operations-health` proves `active_backend = postgres`.
3. Render creates and deploys the API service from the updated blueprint.
4. Render creates and deploys the background worker service.
5. Render creates and executes the cron jobs.
6. Worker heartbeat is visible without opening the mobile app.
7. At least one scheduled research job completes while the app is closed.
8. Alpaca paper research produces either a valid submitted paper order or a persisted no-trade reason.
9. Kraken polling continues without weakening live controls.
10. Automatic reports are generated by cron and visible in the app.

## Blockers

These gates require external Render/Supabase account access and cannot be honestly completed from source-code inspection alone.

Additional local blocker: the Render CLI is not installed or not on PATH in this environment, so service creation/deployment evidence cannot be collected here.

## Rule

AI Trader should remain activation-ready, not production-certified, until every gate above is closed with persisted evidence.
